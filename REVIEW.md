# Reduced publication candidate — review only

The candidate contains a content-only overlay of changed source, tests and runbooks compared with `fcd2970`. It also contains a new public capability summary and an MP4-only privacy screen. No publication, Git mutation, remote execution, source execution or new experiment occurred while preparing this directory.

The allowlist contains **36 source/test/runbook files**: 35 verbatim copies and one explicitly identified documentation derivative. The training runbook's obsolete environment-specific statement was replaced with the verified, narrowly scoped P10 mechanics result and a relative link to `PUBLIC_STATUS.md`. Executed source files and private execution records were not edited. An additional README derivative from the public base replaces dated host-specific prose with a current PUBLIC_STATUS.md link; the already-public historical docs/STATUS.md is unchanged. One newly untracked baseline-analysis source was conservatively excluded; its inclusion would need a separate snapshot review.

Every copied text file was scanned for personal home paths, private hardware identifiers, account names, non-loopback address literals, hardware addresses, private keys and credential-shaped strings. All 28 Python source/test files parse successfully. No test suite or model run is claimed by this preparation. Source hashes identify copied bytes, not proof that every changed file was executed.

Generic loopback addresses remain in the networking implementation and synthetic local socket tests. Generic operating-system paths and clearly labeled placeholder paths remain where code or examples require them. These are not values collected from a user host. The public status page and revised README contain no network addresses, private filesystem paths, hardware identifiers or host diagnostics. The P01/P02/P05 rows state only bounded verified capabilities and their limits; original diagnostic reports are not copied.

The status summary retains the relevant limitations: historical failed reset variants, separate reset/runtime identities, a navigation-success criterion that does not establish touchdown, an unscored interrupted attempt and a fresh restart, and one P10 optimizer exposure without a demonstrated improvement. P09 expert correction remains incomplete; unknown label horizon/frame and the P12 supervision restriction remain explicit.

## Excluded material

No raw log or event stream, runtime receipt, host inventory, experiment configuration, model checkpoint, dataset asset, original report or full release bundle is copied. No videos are copied into this directory. The video audit lists exact existing MP4 hashes for a separately reviewable route; it does not authorize or perform upload.

Eight observation MP4s passed the visible-content/container screen. The actual source slides and rendered fields were inspected, and existing complete static-frame proofs were checked against current MP4, source-slide and mapping hashes. The P10 raw-log MP4 is excluded because its final stdout segment visibly contains a private output path and its optimizer segment exposes runtime memory diagnostics. See [VIDEO_AUDIT.md](VIDEO_AUDIT.md) for exact hashes and [PUBLICATION_MANIFEST.json](PUBLICATION_MANIFEST.json) for coverage.

A video-only content screen does not approve its surrounding report, source mapping or release bundle. A generic `report.html` footer in an MP4 is not an attachment or a public link to the private report. The screen is not a steganographic or legal-rights audit, and publication authorization is not asserted.

## Applying a reviewed candidate

Review the explicit allowlist and payload hashes. If the publication route is authorized, apply only those reviewed file contents to a suitable branch based on the approved public base. Do not push or transplant current local commit history merely because these files passed a content screen: this candidate does not audit that history or the unchanged base repository. The overlay depends on existing public-base files and is not a standalone clone.

The manifest records original source hashes and copied/derived hashes separately. `checksums.sha256` covers all regular files in this candidate except itself. No symlink, binary, private bundle or hidden repository directory is included. Corrections should produce a newly reviewed payload; no automatic approval or blanket public-safety claim is made.
